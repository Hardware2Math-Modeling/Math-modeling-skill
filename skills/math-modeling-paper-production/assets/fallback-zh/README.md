# 通用中文 fallback 模板

此资产只用于验证 `ctexart` 中文 LaTeX 装配和编译，并生成可审阅候选 PDF。它不是任何赛事的官方模板，固定标记为 `fallback_non_submission`，不得直接提交，也不得据此声明 `submission-ready`。

正文范围由编译产物中的 `mm-body-start` 与 `mm-body-end` 标签确定；`BODY_START` / `BODY_END` 注释仅是装配边界。提交前必须换用用户提供或已核验的当届官方模板，并重新完成编译、引用、页数和视觉 QA。

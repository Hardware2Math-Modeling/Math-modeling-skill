interface:
  display_name: "Mathematical Model Validation"
  short_description: "Test model results against explicit validation gates"
  default_prompt: "Use $math-modeling-validation to test these model results against explicit gates."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Math Modeling Orchestrator"
  short_description: "Route modeling work through auditable evidence gates"
  default_prompt: "Use $math-modeling-orchestrator to route this mathematical modeling problem through current evidence and competition gates."
policy:
  allow_implicit_invocation: true
